name: Supabase Keep Alive

on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s -o /dev/null -w "Status: %{http_code}\n" \
            -H "apikey: sb_publishable_qkxySw2VYPsaxFE-18CJSw_BkxfO18L" \
            "https://uiuetruaahjwkdalqnff.supabase.co/auth/v1/health"
